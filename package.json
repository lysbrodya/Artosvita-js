{
  "name": "artosvita",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sass:build": "sass src/styles/main.scss dist/main.css",
    "sass:min": "sass src/styles/main.scss dist/main.min.css --style=compressed",
    "sass:all": "npm run sass:build && npm run sass:min",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "glob": "^13.0.1",
    "prettier": "^3.8.1",
    "sass": "^1.97.3",
    "vite": "^7.2.4"
  }
}
